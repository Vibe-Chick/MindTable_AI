# 백엔드/프론트 전달 사항

`/docs` (Swagger) 로 스키마는 다 보이므로, 여기에는 **스키마만 봐서는 모르는
것**만 적는다. 전부 어기면 에러 없이 품질만 무너지는 종류다.

---

## 1. `/match/run` — `tags` 를 반드시 채워 보낼 것

매칭의 관심사 유사도는 **고정 12종 태그 공간** 위에서만 계산된다.
`interest_tags`(자유 키워드)는 카드 표시용이라 유사도에 쓰이지 않는다.

```jsonc
{
  "candidates": [{
    "name": "...",
    "self_report_vector": { "openness": 4, ... },
    "behavior_corrected_vector": { "openness": 0.0, ... },   // 신규는 전부 0

    "interest_tags": ["홈서버", "리눅스", "재즈 LP"],   // 표시용
    "tags": ["기술", "음악"],                          // ★ 유사도 계산용
    "interest_weights": { "기술": 1.0, "음악": 1.41 },  // 리뷰로 학습된 값
    "diversity_beta": 0.5
  }]
}
```

- `tags` / `interest_weights` 가 **둘 다 비면 그 사용자는 모든 상대와 유사도 0**
  이 되어 사실상 무작위 배정된다. 에러는 나지 않는다.
- `interest_weights` 만 비면 `tags` 로 자동 폴백된다(전부 가중 1.0).
- 값은 `/profile/extract` 응답에 그대로 들어있다. **가공하지 말고 실어 보낼 것.**

허용 태그 12종:

```
운동  여행  요리  음악  영상  독서  게임  기술  학술  창작  봉사  재테크
```

목록에 없는 값은 조용히 무시된다.

### 응답의 `warnings` 를 확인할 것

위 문제를 잡기 위해 `/match/run` 응답에 `warnings: string[]` 를 추가했다.

```jsonc
{ "groups": [...], "leftover": [...], "warnings": [] }
```

**빈 배열이 아니면 매칭 품질에 문제가 있는 것이다.** 개발 중에는 콘솔에
찍어두면 좋다.

---

## 2. 리뷰 강제선택은 정확히 세 문자열

자유 텍스트를 보내면 다양성 선호(`diversity_beta`) 학습이 통째로 무시된다.

```
"더 비슷"  |  "지금 정도"  |  "더 달라도 됨"
```

버튼 라벨을 바꾸더라도 **전송값은 이 세 개 중 하나여야 한다.**

---

## 3. 리뷰 자유서술 문항은 이 형태를 유지할 것

문항이 구체적이지 않으면 관심사 학습이 안 된다. 그냥 "어떠셨나요?" 로 바꾸면
태그 신호가 안 나와서 보정 루프의 절반이 죽는다.

| | 문항 | 이게 없으면 |
|---|---|---|
| R1 | 대화가 제일 잘 풀린 순간이 언제였나요? **무슨 얘기 중이었나요?** | 관심사 태그 가중 학습 불가 |
| R2 | 말이 끊기거나 어색했던 주제가 있었나요? | 태그 하향 신호 없음 |
| R3 | 본인은 주로 이끄는 쪽이었나요, 듣는 쪽이었나요? | 외향성 보정 불가 |

R1 의 "무슨 얘기 중이었나요"가 핵심이다. 그 답에서 **실제로 통한 주제**를
뽑아 유사도 행렬을 갱신한다.

**사람을 평가하는 문항은 넣지 말 것.** 상대방 평점이 아니라 자리를 평가한다.
갱신 대상은 항상 리뷰를 쓴 본인의 프로필이다.

---

## 4. 무대 시연 직전

```
POST /admin/cache-only  { "enabled": true }
```

캐시에 없는 요청이 에러가 된다. 네트워크가 죽어도 데모가 안 죽는다.
**반드시 발표 전에 한 번은 이 상태로 전체 흐름을 돌려볼 것.**
