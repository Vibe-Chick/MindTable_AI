# --- 서버 --------------------------------------------------------------
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.9

# --- 수치 연산 ------------------------------------------------------------
numpy>=1.26

# 통합(INTEGRATION_REPORT.md) 이후 LLM 호출(ai_engine/llm_client.py)은 김주환 브랜치
# 방식대로 urllib(표준 라이브러리)만으로 동작한다. anthropic/openai/voyageai SDK는
# 더 이상 필요하지 않다 - 의존성이 줄어든 것은 의도된 변경이다.
