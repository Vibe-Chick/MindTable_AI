# --- 서버 --------------------------------------------------------------
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.9

# --- LLM / 임베딩 --------------------------------------------------------
anthropic>=0.73          # 순정 Anthropic API 호출 (ai_engine/llm_client.py)
openai>=1.0               # OpenAI 호환 게이트웨이 호출 + OpenAI 임베딩 폴백
voyageai>=0.3             # 임베딩 1순위 폴백. 없어도 해시 기반 폴백으로 동작함(선택)

# --- 수치 연산 ------------------------------------------------------------
numpy>=1.26
