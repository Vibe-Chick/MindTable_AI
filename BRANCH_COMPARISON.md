# jaebin vs 김주환 — AI 모듈 비교 분석 보고서

작성 기준: `origin/jaebin` (4121c1b 시점 없음, jaebin 단일 커밋 b115342) vs
`origin/김주환` (4121c1b, 28개 커밋). `git worktree`로 각각 `mind_table`(jaebin
자체)과 `../review-kimjuhwan`에 분리 체크아웃하여 코드를 직접 읽고 비교했다.
**이 문서는 분석 전용이며, 어떤 코드도 수정하지 않았다.**

---

## 0. 근본적인 아키텍처 차이 (먼저 읽을 것)

두 사람은 "무엇을 만들지"는 같게 이해했지만 "어떻게 서빙할지"를 정반대로
설계했다. 이건 기능 비교 이전에 팀이 먼저 결정해야 하는 문제다.

| | jaebin | 김주환 |
|---|---|---|
| 서빙 방식 | **FastAPI HTTP 서버** (`ai_engine/server.py`) | **서버 없음.** `AI/out/*.json` 정적 파일이 곧 API (`FRONTEND.md`) |
| 프론트 연동 | REST 엔드포인트 호출 | 파이프라인 실행 후 생성된 JSON을 fetch |
| LLM 호출 재사용 | 없음 (매 요청마다 호출) | **디스크 캐시** (`cache/*.json`, 프롬프트 해시 키) — 재실행이 공짜 |
| 무대 시연 시 네트워크 | 필요 (또는 규칙 기반 폴백으로 대체) | `LLM_CACHE_ONLY=1`로 **네트워크 차단 보장** 가능 |
| 성격 벡터 키 | `openness/conscientiousness/...` (영문 풀네임) | `O/C/E/A/N` (단일 문자) |
| 관심사 표현 | `interest_tags` 하나 | `interests`(표시용 자유 키워드) + `tags`(유사도 계산용 고정 12종) 이원화 |
| LLM 프로바이더 | Anthropic SDK / OpenAI 호환 게이트웨이 2모드 | openai/anthropic/gemini 3종 + 게이트웨이 스키마 무시 감지 |

두 구현 모두 "국민대 게이트웨이"를 실제로 겨냥했지만, 김주환 쪽은 **그
게이트웨이가 `response_format: json_schema`/`json_object`를 에러 없이 무시하고
`tools` 방식만 동작한다는 사실을 `probe.py`로 직접 검증하고 코드에 반영**했다
(`llm.py`, `LLM_STRUCTURED=tools` 기본값). jaebin 쪽은 이 문제를 겪지 않았는데,
이는 jaebin이 OpenAI 호환 `/chat/completions`의 일반 텍스트 응답을 받아 자체
`_extract_json_object`로 파싱하는 방식이라 애초에 `response_format`을 요청하지
않기 때문이다 — 같은 문제를 다른 회피 경로로 피해간 것이므로 우열이 아니라
**서로 다른 방어선**이다.

---

## 1. 파일 구조

```
jaebin (mind_table/)                    김주환 (AI/)
├─ ai_engine/                           ├─ src/
│  ├─ llm_client.py    LLM 호출 추상화   │  ├─ llm.py          LLM 호출+캐시+재시도
│  ├─ json_utils.py    JSON 파싱 방어    │  ├─ prompts.py       프롬프트 분리
│  ├─ schemas.py       dataclass 정의    │  ├─ schema.py        데이터계약+튜닝상수
│  ├─ profile.py       프로필 추출       │  ├─ extract.py       ★LLM#1 프로필 추출
│  ├─ matching.py      매칭+카드생성     │  ├─ embed.py         유사도 행렬
│  ├─ restaurant.py    식당 추천         │  ├─ match.py         편성 최적화(비LLM)
│  ├─ feedback.py       리뷰 보정 루프   │  ├─ cards.py         ★LLM#2 카드(식당 포함)
│  ├─ api_models.py    Pydantic 모델     │  ├─ review.py        ★LLM#3 보정 루프
│  ├─ store.py         인메모리 저장소   │  ├─ mock.py          합성 프로필(숨은정답)
│  ├─ server.py        FastAPI 앱       │  ├─ metrics.py       발표 지표 집계
│  └─ demo.py          데모+mock데이터   │  ├─ simulate.py      수렴 검증+ablation
├─ test_extract.py     단독 테스트       │  ├─ verify.py        8시드 재현성 검증
                                         │  ├─ tune.py          하이퍼파라미터 그리드서치
                                         │  ├─ sweep_gamma.py   민감도 분석
                                         │  └─ compare_models.py 모델 선택 근거 측정
                                         ├─ run.py / run_real.py   전체 파이프라인 실행
                                         ├─ round_close.py     회차 마감(리뷰→갱신→편성)
                                         ├─ make_reviews.py    리뷰 템플릿 생성
                                         ├─ compare_rounds.py  회차간 변화 비교
                                         ├─ anonymize.py       샘플 데이터 익명화
                                         ├─ probe.py           게이트웨이 응답방식 진단
                                         ├─ check_env.py       환경변수/모델목록 점검
                                         ├─ repair_interests.py 버그 복구 스크립트
                                         └─ data/, out/        입출력 데이터 (일부 샘플)
```

- jaebin: **13개 파일, 2,351줄**, 모듈 8개 + 서버/데이터모델/저장소 3개
- 김주환: **28개 파일, 2,918줄**, 파이프라인 모듈 15개 + CLI 도구 9개 + 데이터/샘플

김주환 쪽 파일 수가 많은 이유는 파이프라인 모듈 자체보다 **검증/튜닝/진단
도구**(`verify.py`, `tune.py`, `sweep_gamma.py`, `compare_models.py`,
`probe.py`, `check_env.py`)가 별도 스크립트로 분리되어 있기 때문이다.

---

## 2. 기능 대응관계 매핑

| 기능 | jaebin | 김주환 |
|---|---|---|
| 프로필 추출 (설문→Big Five+관심사) | `profile.py: extract_profile` | `src/extract.py: extract_one` (+ `src/prompts.py`) |
| LLM 클라이언트 추상화 | `llm_client.py: _call_llm/_get_chat_backend` | `src/llm.py: call/_build/_parse_*` |
| 매칭 알고리즘 | `matching.py: pair_score/form_groups` | `src/match.py: score_group/greedy/local_search` + `src/embed.py` |
| 매칭 이유/아이스브레이커 | `matching.py: generate_match_reason/generate_icebreakers` | `src/cards.py: build_card` |
| 식당 추천 | `restaurant.py: resolve_restaurant` (좌표+예산 기반) | `src/cards.py: pick_restaurant` (하드코딩 6곳) |
| 리뷰 기반 피드백 루프 | `feedback.py: run_feedback_pipeline` | `src/review.py: extract_delta/apply_delta` |
| API 서버 레이어 | `server.py` (FastAPI, 4개 엔드포인트) | **없음** (정적 JSON 산출, `run.py`가 오케스트레이터) |
| Mock 데이터/데모 | `demo.py: generate_mock_users` (8명 고정) | `src/mock.py: gen` (N명 합성 + 숨은 정답 내장) |
| 에러처리/JSON파싱방어/재시도 | `json_utils.py` + 각 모듈 try/except+재시도 | `llm.py`(재시도+캐시) + `extract.py`/`cards.py`(인용 대조·타겟 검증) |
| (김주환 전용) 하이퍼파라미터 검증 | — | `simulate.py`, `verify.py`, `tune.py`, `sweep_gamma.py` |
| (김주환 전용) 모델 선택 근거 측정 | — | `compare_models.py` |
| (jaebin 전용) 반복 매칭 이력 저장소 인터페이스 | `store.py: get_match_history/save_match_history` | `profile["met"]/["history"]`에 내장 |

---

## 3. 항목별 심층 비교

각 항목: 완성도 / 견고성 / AI 활용 깊이 / 코드 구조 / 성능 / 데모 안정성 /
일관성 순으로 평가. 점수는 5점 만점, 상대 비교용.

### 3.1 프로필 추출 (Big Five + 관심사)

| 기준 | jaebin | 김주환 |
|---|---|---|
| 완성도 | 4 — 5축+관심사 3개, few-shot 2개 | 5 — 5축+관심사(1~4개, 유동)+고정태그 3종 동시 추출 |
| 견고성 | 3 — JSON 구조 검증(범위/타입)만 | **5** — **인용문을 답변 원문과 문자열 대조**, 근거 없으면 점수 3점으로 강제 |
| AI 활용 깊이 | 3 — 단순 프롬프팅 | 4 — 근거 강제 + 축 간 교차 오염 금지 지시("다른 축 근거 끌어쓰지 마라") |
| 코드 구조 | 4 — 단일 함수, 명확 | 4 — CSV 컬럼 매핑, 근거 대조 로직 분리 |
| 성능 | 3 — 순차 1콜/명 | 4 — `ThreadPoolExecutor` 기반 배치 병렬 (`WORKERS=4`) |
| 데모 안정성 | 4 — 키 없으면 해시 기반 폴백 프로필 | 4 — `LLM_STUB=1`(개발용) / `LLM_CACHE_ONLY=1`(무대용) 이원화 |
| 일관성 | 4 | 4 |

**핵심 차이**: 김주환은 "모델이 인용문이랍시고 요약을 내놓는다"는 실제로 관찰된
실패를 **문자열 포함 여부 검사**로 코드에서 강제 차단한다(`extract.py:
_norm(quote) in src`). jaebin은 이 단계의 방어가 없어 모델이 답변에 없는 근거를
지어내도 걸러내지 못한다. 이건 순수 "환각 방지" 기법으로, 두 팀 다 "5점 만점
1~5 정수 스키마"는 동일하게 잘 만들었지만 **근거 검증 유무**가 갈린다.

**jaebin 우위 지점**: few-shot 예시가 구조화되어 있고, `_extract_json_object`
로 코드블록/잡담 섞인 응답을 방어하는 처리가 김주환의 `_json_or_die`(거의
동일한 접근: `{`~`}` 슬라이스)와 대등하다 — 이 부분은 사실상 동점.

---

### 3.2 LLM 클라이언트/백엔드 추상화

| 기준 | jaebin | 김주환 |
|---|---|---|
| 완성도 | 3 — Anthropic / OpenAI 호환 게이트웨이 2모드 | **5** — openai/anthropic/gemini 3벤더 + 벤더별 파서 |
| 견고성 | 3 — 일반 예외 기반 재시도(모듈마다 개별 구현) | **5** — HTTP 429/5xx 지수백오프, `ParseError`(재시도 무의미)와 일반 오류 구분, "필수키 전부 누락=게이트웨이가 스키마 무시" 자동 감지 |
| AI 활용 깊이 | 3 | 4 — 구조화 출력을 `tools`/`json_schema`/`json_object` 3방식으로 전환 가능하게 설계, 게이트웨이 특성에 맞춰 선택 |
| 코드 구조 | 4 — 모듈 분리 깔끔 | 4 — 의존성 0 (stdlib만), 40줄이라는 자체 평가는 다소 과소평가지만 응집도 높음 |
| 성능 | 3 | **5** — **디스크 캐시**로 동일 프롬프트 재호출 0회, `map_call`로 동시성 제한 배치 |
| 데모 안정성 | 4 — 규칙 기반 폴백 | **5** — `LLM_CACHE_ONLY=1`로 "무대에서 네트워크 요청 0회"를 코드 레벨에서 강제 가능 |
| 일관성 | 4 | 3 — 프로바이더별 모델 기본값에 `SET_LLM_MODEL` 같은 플레이스홀더가 남아있어 미설정 시 런타임 에러(의도적 fail-fast이긴 함) |

**핵심 차이**: 캐시의 유무가 가장 크다. jaebin은 요청마다 LLM을 호출하므로
- 개발 중 반복 실행 = 매번 과금/지연
- 데모 리허설 = 매번 네트워크 의존 + 응답 변동성(비결정적 문구)

김주환은 프롬프트+스키마+모델을 해시한 키로 결과를 `cache/*.json`에 영구
저장하고, `LLM_CACHE_ONLY=1`이면 캐시 미스를 **에러로 즉시 중단**시켜 "무대에서
실수로 실제 API를 호출하는 상황" 자체를 차단한다. 이건 해커톤 발표 환경을
정확히 겨냥한 설계다.

**jaebin 우위 지점**: 코드가 더 읽기 쉽고 (특히 Anthropic Messages API 스키마를
정확히 반영), 학교 게이트웨이가 아닌 순정 Anthropic API를 쓰는 경로도 이미
검증됨(`ANTHROPIC_BASE_URL` 지원). 김주환은 Anthropic 순정 경로는 코드만
있고(`_build`의 `anthropic` 분기) 실제로 호출해본 흔적(캐시/커밋 로그)이
없다 — 게이트웨이 openai 호환 경로 위주로 실전 검증됨.

---

### 3.3 매칭 알고리즘 (유사도+다양성 스코어링, 그룹 형성, 반복매칭 방지)

| 기준 | jaebin | 김주환 |
|---|---|---|
| 완성도 | 4 — 성격 유사도(단일 유클리드)+관심사 코사인+다양성, 그리디 병합 | **5** — 성격 유사도(축별 차등: 외향성=분산 선호, 개방성/우호성=유사 선호)+관심사(태그 코사인+자카드)+전공/단과대/학교 3단 다양성, greedy+local search 2단계 |
| 견고성 | 3 | 4 — `assert local >= greedy >= random`을 코드에 내장해 최적화기 퇴행을 자동 감지 |
| AI 활용 깊이 | 3 — 임베딩 API 우선, 실패시 해시 폴백 | 4 — **임베딩 없는 게이트웨이를 전제로 고정 태그 공간(12차원) 설계**, 임베딩 있으면 0.6/0.4 가중 합성. "왜 이 설계인가"에 대한 근거(게이트웨이에 임베딩 모델 자체가 없음)가 더 명확 |
| 코드 구조 | 4 | 4 |
| 성능 | 3 — 그리디 1패스 | 4 — 그리디 + 국소 탐색(스왑) 반복 개선, 반복 상한으로 시간 보장 |
| 데모 안정성 | 4 | 4 — mock 데이터에 "숨은 정답" 내장해 최적화기 품질을 랜덤 대비 수치로 항상 증명 가능 |
| 일관성 | 4 | 4 |

**핵심 차이 1 — 반복 매칭 페널티 소비하는 항의 차이**: jaebin의
`UserProfile.diversity_beta`(리뷰 강제선택으로 학습)는 **`feedback.py`에서
계산만 되고, `matching.py`의 `pair_score`는 이 값을 전혀 읽지 않는다.**
`w_diversity=0.3`은 항상 고정값이다. 즉 "다양성 선호도를 개인화 학습한다"는
기능이 **절반만 배선되어 있다** — 배선 안 된 죽은 값이다. 김주환은
`score_group`에서 `beta = mean(members의 beta)`로 실제 목적함수에 즉시
반영한다. 이건 완성도 관점에서 명확한 jaebin 쪽 결함이다.

**핵심 차이 2 — 성격 유사도의 심리학적 모델**: jaebin은 5축을 하나의 유클리드
거리로 뭉쳐 "가까울수록 좋다"로 단순화한다. 김주환은 **외향성은 분산이 클수록
좋고(전원 조용하거나 전원 시끄러우면 자리가 죽는다는 관찰), 개방성/우호성은
유사할수록 좋다**로 축마다 다른 함수를 쓴다. 두 설계 모두 "적정
차별성"이라는 같은 이론적 뿌리(jaebin 문서상 Optimal Distinctiveness
Theory)를 겨냥하지만, 김주환 쪽이 심리학적으로 더 섬세하다.

**핵심 차이 3 — 반복 매칭 페널티 정책**: jaebin은 `match_history`의 실제 매칭
"횟수"에 비례해 -0.1×횟수(최대 -0.3)로 완만하게 감점한다. 김주환은
`met` 리스트에 있으면(몇 번 만났든) 그 쌍마다 고정 -0.8을 감점한다 — 그룹
안에 반복 쌍이 2개면 -1.6까지 깎인다. 이는 "한 번이라도 만났으면 강하게
피한다"는 훨씬 공격적인 정책이다. 어느 쪽이 맞는지는 데이터 없이는 판단
불가하나, **jaebin 쪽이 반복 횟수를 실제로 카운트한다는 점에서 더 정교한 입력을
갖고 더 단순한 정책을 쓰고, 김주환은 더 단순한 입력(이진 여부)으로 더 공격적인
정책을 쓴다** — 병합 시 반드시 팀이 하나를 골라야 한다.

**jaebin 우위 지점**: `store.py`의 "함수 인터페이스 뒤에 상태를 숨긴다" 설계는
매칭 이력을 프로필과 분리된 독립 테이블로 다루므로, 실제 DB로 옮길 때
(예: `match_history` 테이블) 더 자연스럽다. 김주환은 이력이 각 프로필 안에
내장돼 있어(`met`, `history`) 사용자 문서 자체가 계속 커지고, 이력만 별도로
질의하기 어렵다.

---

### 3.4 매칭 결과 설명/아이스브레이커 생성

| 기준 | jaebin | 김주환 |
|---|---|---|
| 완성도 | 4 | **5** — reason + icebreakers(질문+`targets`) + `overlap` + `restaurant`까지 카드 하나로 완결 |
| 견고성 | 3 — "문자열 3개 리스트인가"만 검증, 실패시 규칙 기반 폴백 | **5** — **① 1번 질문은 전원이 targets에 포함되는지 ② 2·3번은 최소 2명인지 ③ 세 질문 targets 합집합이 전원을 커버하는지 ④ 질문에 프로필의 구체적 활동 키워드가 실제로 등장하는지**를 코드로 검증, 실패시 구체적 오류를 프롬프트에 넣어 **1회 재작성** 요청 |
| AI 활용 깊이 | 3 — 프롬프트에 "범용 질문 금지"만 지시 | **5** — 나쁜 예/좋은 예 다수 제시, "예/아니오 질문 금지", "1인 전용 질문 금지"를 프롬프트+코드 이중 방어 |
| 코드 구조 | 4 | 4 |
| 성능 | 2 → **이번 세션에 병렬화 완료 (아래 참고)** | 3 — `build_all`이 그룹을 **순차** 처리 (병렬화 없음) |
| 데모 안정성 | 4 — 폴백 템플릿 문장 | 4 — LLM 실패시 `reason: None, icebreakers: []`로 카드 자체는 항상 생성 |
| 일관성 | 4 | 4 |

**핵심 차이**: 김주환의 `_check()` 검증은 이 프로젝트에서 가장 정교한 방어
로직이다. jaebin의 아이스브레이커는 "3개의 문자열 배열"이기만 하면 통과되므로,
모델이 "취미가 뭐예요?" 같은 범용 질문이나 특정 1인만 답할 수 있는 질문을
내놓아도 걸러내지 못한다(프롬프트로만 금지). 김주환은 이걸 구조적으로 막는다.

**jaebin 우위 지점(이번 세션에 직접 검증)**: jaebin의
`run_matching_pipeline`은 이번 요청으로 `ThreadPoolExecutor(max_workers=8)`
병렬화를 적용해, 그룹 2개(호출 4개) 기준 순차 1.601초 → 병렬 0.417초
(**3.84배**)를 인위적 지연 벤치마크로 실측했다. 김주환의 `cards.build_all`은
그룹을 for 루프로 순차 처리하며 병렬화가 전혀 없다 — 그룹 수가 많아지면
jaebin 쪽 성능 우위가 커진다. 단, 김주환의 `llm.py`가 `WORKERS=4`로 동시성을
의도적으로 제한한 이유("신규 키는 동시 요청 많으면 429")를 감안하면, jaebin의
`max_workers=8`은 같은 게이트웨이에서 429를 유발할 위험이 있다 — **병합 전에
실측 확인이 필요하다.**

---

### 3.5 식당 추천 (위치/예산 기반)

| 기준 | jaebin | 김주환 |
|---|---|---|
| 완성도 | **5** — 중간지점(centroid)+예산 교집합+반경 조건 완화(최대 3단계)+haversine 거리 | 2 — 하드코딩 6곳 중 "구성원 학교가 근처인 곳, 동률이면 저렴한 곳" 1줄 로직 |
| 견고성 | **5** — 위치 정보 전무시 `ValueError`, 후보 0개면 반경/예산을 단계적으로 완화, 그래도 없으면 `restaurant: None` + 명시적 메시지 | 2 — 조건 완화 없음, 매칭 실패 케이스 자체가 없음(캠퍼스 상권 6곳 중 무조건 하나는 나옴) |
| AI 활용 깊이 | 3 — LLM 미사용(규칙 기반), 이 항목 자체는 AI 요소 없음 | 3 — 동일하게 규칙 기반 |
| 코드 구조 | **5** — `compute_midpoint/compute_budget_intersection/query_restaurants/resolve_restaurant` 책임 분리 | 3 — `pick_restaurant` 단일 함수 |
| 성능 | 4 | 5 (단순하니 당연히 빠름) |
| 데모 안정성 | 4 — 이번 세션에 mock DB를 3곳→14곳으로 확충, 8명 중 4명 조합 70가지 전부 완화 0단계로 검증 완료 | 4 — 카테고리/가격 표기가 있어 카드 UI에 바로 쓰기 좋음(`"1.5만"` 같은 표시용 포맷) |
| 일관성 | 3 — 자체 API 계약(`{restaurant, midpoint, budget_range, relaxed_steps, message}`) | 3 — `FRONTEND.md`에 이미 계약(`{name, category, price, near}`)이 명시되어 **프론트가 이미 이 형태를 참고 중일 가능성** |

**핵심 차이**: 이 항목은 **jaebin이 명확히 우위**다. 실제 좌표 기반 거리 계산,
예산 교집합, 단계적 조건 완화까지 갖춘 완결된 로직이며 이번 세션에 mock
데이터까지 현실적으로(국민대 미래관 반경) 보강해 70개 조합 전수 검증을
마쳤다. 김주환 쪽은 README에 "해커톤 기간에 제휴는 못 딴다. 데모는 이걸로
충분하다"라고 스스로 명시한 대로 의도적 스텁이다.

단, **`FRONTEND.md`의 카드 스키마가 이미 김주환의 필드명(`name/category/
price/near`)을 기준으로 문서화되어 있다는 점**은 무시할 수 없다 — 프론트엔드
팀이 이미 이 계약을 보고 작업 중이라면, jaebin의 로직(우수함)을 가져오되
**출력 필드명은 김주환의 계약에 맞춰야** 프론트 재작업이 없다.

---

### 3.6 리뷰 기반 피드백/프로필 보정 루프

| 기준 | jaebin | 김주환 |
|---|---|---|
| 완성도 | 4 — 델타 추출→클립→누적→다양성 β 갱신→괴리 계산→임계 이벤트 | **5** — 위 전부 + **관심사 가중치 학습(태그 단위)까지 닫힌 루프**, confidence 가중 |
| 견고성 | 4 — 24시간 타임아웃/2회 실패 시 델타 0 처리 | 4 — 미제출/추출 실패시 델타 0(동일 철학), 단 jaebin처럼 "hours_since_meal" 명시적 파라미터는 없고 "리뷰 파일에 값이 있는지"로 판단 |
| AI 활용 깊이 | 3 — 델타(-1~1 연속값)+인용 | 4 — 델타(이산 5단계)+confidence+**worked_topics/dead_topics**(대화가 실제로 통한/죽은 주제)까지 추출해 관심사 유사도 공간을 직접 갱신 |
| 코드 구조 | 4 | 4 |
| 성능 | 4 | 4 |
| 데모 안정성 | 3 — 데모 스크립트에서 델타 누적을 수동으로 6회 반복시켜 임계값 초과를 인위적으로 검증 | **5** — `mock.py`가 **숨은 정답**을 심어두고, `simulate.py`가 "보정 루프가 실제로 그 정답에 수렴하는가"를 `assert`로 검증, `verify.py`가 8개 시드로 재현성까지 확인 |
| 일관성 | 4 | 4 |

**핵심 차이 1 — 보정 갱신 공식**: jaebin은 `behavior += clip(delta*0.5, cap=
현재까지 누적량 기준 잔여 여유)` — **감쇠가 없는 순수 누적+상한 클립**이다.
김주환은 `behavior = behavior*(1-0.10) + LR*confidence*delta` — **매 회차
기존 값을 10%씩 감쇠시킨 뒤 새 신호를 더하는 지수이동평균**이다. 김주환의
`schema.py` 주석에 따르면 이 감쇠가 없으면 "리뷰 잡음이 랜덤워크로 누적되어
이미 정확했던 사람의 프로필까지 망가진다"는 것을 **실제 시뮬레이션으로
확인하고** 추가한 것이다. jaebin의 방식은 한 번 상한 근처까지 가면 반대
방향 신호가 없는 한 계속 그 근처에 머무르므로, **일시적 노이즈 하나가 영구적
편향으로 남을 위험이 있다** — 이는 검증되지 않은 채 남아있는 잠재적 결함이다.

**핵심 차이 2 — 관심사 학습 루프의 완결성**: 이게 가장 실질적인 차이다.
jaebin의 리뷰 파이프라인은 Big Five 벡터와 `diversity_beta`만 갱신한다.
**관심사(`interest_tags`)는 온보딩 이후 한 번도 갱신되지 않는다** — 즉
`pair_score`의 관심사 코사인 유사도 항은 몇 회차가 지나도 항상 첫 온보딩
데이터로만 계산된다. 김주환은 리뷰에서 "실제로 대화가 터진 주제"를 추출해
`interest_weights`를 직접 갱신하고, 이게 곧바로 `embed.py`의 유사도 행렬에
반영된다. README가 스스로 명시하듯 "**이게 유사도 행렬을 갱신하는 유일한
경로**"다. jaebin은 이 경로 자체가 없다 — 리뷰 기반 피드백 루프의 "완성도"
측면에서 **김주환이 명확히 앞선다.**

**핵심 차이 3 — 검증 방법론**: jaebin의 데모는 delta=1.0을 인위적으로 6번
주입해 "clip이 1.5에서 멈추는지, 이벤트가 True가 되는지"를 확인하는 **단위
테스트 수준**이다. 김주환은 숨은 정답이 있는 합성 데이터로 "**보정 루프가
자기보고가 틀린 사람의 오차를 실제로 줄이는가(수렴)**"를 8개 시드에 걸쳐
통계적으로 검증하고, 심지어 "보정을 껐을 때와 켰을 때"를 대조하는 ablation까지
수행한다. 이건 같은 기능의 검증 "깊이"가 다른 차원이다.

---

### 3.7 API 서버 레이어

| 기준 | jaebin | 김주환 |
|---|---|---|
| 완성도 | 4 — `/health`, `/profile/extract`, `/match/run`, `/restaurant/resolve`, `/review/submit` 5개 엔드포인트, CORS, `/docs` 자동생성 | N/A — 서버 자체가 없음(설계상 의도) |
| 견고성 | 4 — 입력 문제 400 / LLM 재시도초과 500 구분, 각 라우트 try/except | N/A |
| 코드 구조 | 4 — Pydantic 모델과 dataclass 간 `to_dataclass()/from_dataclass()` 변환 계층 | N/A |
| 검증 | 이번 세션에 `/match/run`(8명→그룹2개), `/restaurant/resolve`(성공 케이스) 실제 호출로 200 확인 | N/A |

이 항목은 사실상 jaebin에만 존재하는 기능이다. 다만 **김주환이 이걸 만들지
않은 게 "미완성"이 아니라 "정적 파일 계약으로 충분하다"는 의도적 설계
선택**이라는 점(`FRONTEND.md`: "백엔드는 서버가 아니다")을 감안해야 한다. 팀이
실제로 라이브 상호작용(사용자가 브라우저에서 버튼을 눌러 즉시 매칭)이
필요하면 jaebin의 서버가 필수고, "정해진 순서로 파이프라인을 돌려 결과를
보여주는" 데모/발표 형태면 김주환의 정적 JSON 방식이 더 안전하다(네트워크
장애 리스크 0).

---

### 3.8 Mock 데이터 / 데모 스크립트

| 기준 | jaebin | 김주환 |
|---|---|---|
| 완성도 | 4 — 8명 고정, 국민대/숭실대/순천향대 실제 좌표 클러스터링(이번 세션에 보강) | **5** — N명 합성 가능, 전공-관심사 상관관계까지 반영(`MAJOR_AFFINITY`) |
| 검증 활용도 | 3 — 매칭 결과를 눈으로 확인하는 용도 | **5** — 숨은 정답 내장으로 정량적 알고리즘 검증의 **입력 데이터** 역할까지 겸함 |
| 현실성 | 4 — 실제 지리 좌표 사용 | 4 — 실제 서울 소재 대학 6개 사용, 정규분포 기반 성격치 |

---

### 3.9 에러 처리 / JSON 파싱 방어 / 재시도 로직

| 기준 | jaebin | 김주환 |
|---|---|---|
| JSON 추출 방어 | `_extract_json_object`/`_extract_json_array`: 첫 `{`~마지막 `}` 슬라이스 | `_json_or_die`: 코드펜스 제거 후 동일한 슬라이스 방식 — **거의 동일한 해법** |
| 재시도 정책 | 함수마다 개별 루프(`MAX_EXTRACT_ATTEMPTS=3`, `MAX_ICEBREAKER_ATTEMPTS=2`) | `llm.py` 한 곳에 `MAX_RETRY=3` 공통 적용 + **HTTP 상태코드별 분기**(429/5xx만 재시도, 4xx는 즉시 실패) + **"일부 키 누락=재시도, 전체 키 누락=게이트웨이 문제이므로 즉시 포기"** 구분 |
| 재시도 위치 | 각 파이프라인 함수(`extract_profile`, `generate_icebreakers`)에 흩어짐 | `llm.call()` 한 곳에 집중, 모든 호출자가 자동으로 혜택 |

**핵심 차이**: 김주환의 재시도 로직이 구조적으로 더 우수하다. jaebin은
"예외가 나면 재시도"라는 단일 기준이지만, 김주환은 **HTTP 상태 코드로 재시도
가치를 판단**(429/5xx=일시적, 재시도 가치 있음 vs 4xx=구조적 문제, 재시도
무의미)하고, **"응답이 왔지만 스키마가 하나도 안 맞으면 재시도해도 똑같다"는
것까지 구분**해 즉시 포기한다. 이건 불필요한 재시도로 인한 지연/비용을
줄이는 실질적 이점이다.

---

## 4. 기능 단위별 채택 추천

| 기능 | 추천 베이스 | 이유 |
|---|---|---|
| 프로필 추출 | **김주환** | 인용 대조 그라운딩이 핵심 차별점. jaebin의 few-shot 구성은 참고만 |
| LLM 클라이언트 | **김주환** | 캐시+재시도 정책+스키마 무시 감지가 압도적. jaebin의 Anthropic 순정 SDK 분기는 이식할 가치 있음 |
| 매칭 알고리즘 | **김주환** (알고리즘) + **jaebin** (이력 저장 인터페이스) | 목적함수/최적화 단계는 김주환이 우수. 다만 `store.py`식 외부 이력 테이블 패턴을 김주환의 임베디드 `met` 대신 채택 검토 |
| 카드(이유+아이스브레이커) | **김주환** (검증 로직) + **jaebin** (병렬화) | 타겟 커버리지 검증은 필수급으로 좋음. 여기에 jaebin의 ThreadPoolExecutor 병렬 처리를 그대로 이식 |
| 식당 추천 | **jaebin** | 압도적으로 완성도 높음. 단, 출력 필드명은 김주환의 `FRONTEND.md` 계약(`name/category/price/near`)에 맞춰 조정 |
| 리뷰 피드백 루프 | **김주환** | 관심사 학습 루프 완결성 + EMA 감쇠 + 시뮬레이션 검증. jaebin에 없는 기능이 핵심이라 대체 불가급 |
| API 서버 | **jaebin** (필요시) | 라이브 상호작용이 필요하면 유일한 선택지. 단, 캐시 계층(김주환의 `llm.call`)을 얹어야 반복 호출 비용 문제가 안 생김 |
| Mock 데이터/데모 | **김주환** (검증용) + **jaebin** (지리 좌표) | 숨은 정답 시뮬레이션 골격은 김주환, 실좌표 클러스터링 방식은 jaebin 걸 합성 데이터에 이식 |
| 에러처리/재시도 | **김주환** | HTTP 상태코드 기반 재시도 분기를 jaebin의 `_call_llm`에도 이식 권장 |

---

## 5. 특정 브랜치에만 있는, 가져오면 좋은 것

### 김주환 → jaebin으로 이식 권장
1. **디스크 캐시 (`llm.py`의 `call()`)** — 프롬프트+스키마+모델 해시를 키로
   결과를 파일에 저장. jaebin의 `_call_llm`에 이 레이어를 얹으면 개발/데모
   비용이 즉시 줄어든다. `LLM_CACHE_ONLY` 플래그도 함께.
2. **인용 그라운딩 검증** (`extract.py`의 `_norm(quote) in src`) —
   `profile.py`의 `_validate_profile`에 추가하면 환각 점수를 걸러낼 수 있다.
3. **아이스브레이커 타겟 커버리지 검증** (`cards.py`의 `_check`) —
   `matching.py`의 `generate_icebreakers`에 이식하면 "1인 전용 질문"
   문제를 구조적으로 막을 수 있다.
4. **HTTP 상태코드 기반 재시도 분기** — 429/5xx만 재시도, 그 외 즉시 실패.
5. **`probe.py` 자체** — 학교 게이트웨이가 바뀌거나 새 모델로 전환할 때 다시
   돌려볼 진단 스크립트로 그대로 유용하다.
6. **관심사 태그 학습 루프** (`review.py`의 `apply_topics` +
   `embed.py`의 태그 공간 설계) — jaebin의 리뷰 루프에서 가장 크게 빠진
   조각. 이식하려면 jaebin의 `interest_tags`를 김주환처럼
   `interests`(표시용)/`tags`(계산용) 이원화로 스키마 변경이 선행돼야 한다.
7. **EMA 감쇠 기반 behavior 갱신** — `clip_delta`를 대체하거나 병행 옵션으로.

### jaebin → 김주환으로 이식 권장
1. **`restaurant.py` 전체** (좌표 거리+예산 교집합+조건 완화) — 김주환의
   `pick_restaurant`를 대체. 필드명만 `FRONTEND.md` 계약에 맞춰 어댑터
   한 겹 씌우면 된다.
2. **그룹별 카드 생성 병렬화** — `cards.build_all`의 순차 for문을
   `ThreadPoolExecutor`로 교체(단, 김주환의 `WORKERS=4` 제약과 상충하지
   않는지 먼저 실측 확인).
3. **`diversity_beta`를 실제로 목적함수에 연결하는 배선** — 이건 사실
   jaebin 자신에게도 아직 안 된 일이지만, 두 팀 다 이 패턴(개인화 계수를
   실제로 스코어에 곱하는 것)을 참고할 수 있다. 김주환은 이미 `beta`를
   `score_group`에서 쓰고 있으므로 오히려 jaebin이 김주환을 보고 배선을
   완성해야 하는 방향.
4. **Pydantic 기반 API 스키마 계층** (`api_models.py`) — 만약 FastAPI
   서버를 채택한다면 이 변환 계층을 그대로 재사용.

---

## 6. 병합 시 예상 충돌/비일관성 지점

| 영역 | jaebin | 김주환 | 충돌 내용 |
|---|---|---|---|
| 성격 축 키 이름 | `openness`, `conscientiousness`, ... (영문 풀네임) | `O`, `C`, `E`, `A`, `N` (단일 문자) | **모든 프로필 dict의 키가 다름.** 변환 함수 하나로 매핑 가능하지만 전 모듈에 영향 |
| 관심사 표현 | `interest_tags: List[str]` 단일 필드 | `interests`(자유 키워드) + `tags`(고정 12종 분류) 이원화 | 구조 자체가 다름. 김주환 쪽으로 통일 시 jaebin의 `pair_score` 임베딩 로직 전면 수정 필요 |
| 다양성 개인화 계수 | `diversity_beta` (UserProfile 필드, 매칭에 미반영) | `beta` (profile 필드, 매칭에 실반영) | 이름도 다르고 배선 상태도 다름 |
| 환경변수 체계 | `ANTHROPIC_API_KEY`/`ANTHROPIC_BASE_URL`/`ANTHROPIC_MODEL` + `LLM_BASE_URL`/`LLM_API_KEY` (2계층) | `LLM_PROVIDER`/`LLM_MODEL`/`LLM_API_KEY`/`LLM_BASE_URL` (단일 계층, provider로 3벤더 분기) | `.env.example`부터 재통일 필요. 김주환 체계가 더 일반적(provider 추상화)이라 이쪽으로 합치는 게 자연스러움 |
| 반복 매칭 페널티 정책 | 횟수 비례 -0.1×n (상한 -0.3) | 이진 매칭여부 -0.8 (그룹 내 반복쌍마다) | 정책 자체가 다름 — 실측 데이터로 하나를 골라야 함 |
| 매칭 이력 저장 위치 | 외부 `match_history` dict (`store.py`로 추상화) | 프로필 내장 `met`/`history` 리스트 | 아키텍처 선택 문제. DB 이전을 고려하면 jaebin 쪽(정규화된 테이블)이 유리 |
| 식당 추천 응답 스키마 | `{restaurant, midpoint, budget_range, relaxed_steps, message}` | `{name, category, price, near}` (카드에 내장) | 필드명/중첩 구조 전부 다름. `FRONTEND.md`가 이미 김주환 스키마를 문서화했으므로 그쪽 우선 고려 |
| 그룹 결과 스키마 | `{members, match_reason, icebreakers}` | `{group_id, members, reason, icebreakers, icebreaker_targets, overlap, restaurant}` | 김주환 쪽이 필드가 더 많음(타겟, 겹침 키워드 등). 병합 시 상위 호환으로 김주환 스키마를 기준 삼는 게 정보 손실이 적음 |
| 서빙 방식 | HTTP 서버 | 정적 JSON 파일 | **가장 큰 결정 사항.** 프론트엔드 진행 상황에 따라 팀 전체가 논의해야 함 |
| 동시성 상한 | `max_workers=8` (카드 생성) | `WORKERS=4` (전 구간, 429 회피 목적) | 병합 시 실제 게이트웨이 rate limit을 재확인 후 하나로 통일 |

---

## 7. 총평 및 최종 권장안

### 요약
- **김주환 브랜치**: 백엔드 파이프라인 로직(추출/유사도/매칭/카드/보정
  루프) 자체의 **완성도·견고성·AI 활용 깊이·검증 rigor**가 전반적으로
  더 높다. 특히 (1) 관심사 학습이 실제로 유사도 계산에 재반영되는 닫힌
  루프, (2) 숨은 정답 시뮬레이션으로 보정 루프의 수렴을 통계적으로
  검증(8시드), (3) 게이트웨이 응답 형식 문제를 직접 진단하고 코드로
  대응한 흔적(`probe.py`, 커밋 로그)은 jaebin에 대응물이 없다. 다만 이는
  **서버가 없는 정적 파일 아키텍처**를 전제로 한다.
- **jaebin 브랜치**: **API 서버 레이어**와 **식당 추천 로직**은 명확히
  더 완성도가 높고 실사용 가능한 수준이다. 이번 세션에 병렬화까지
  적용되어 그룹 수가 늘어날수록 카드 생성 성능에서 우위를 갖는다. 다만
  LLM 호출 캐시가 전혀 없어 반복 실행/데모 비용이 크고, 리뷰 루프가
  관심사 벡터를 갱신하지 않으며, `diversity_beta`가 실제로는 매칭에
  반영되지 않는 등 "설계는 있는데 배선이 끊긴" 부분이 몇 군데 있다.

### 권장안
1. **핵심 파이프라인(추출/유사도/매칭 목적함수/보정 루프)은 김주환의
   `src/` 코드를 베이스로 가져간다.** 특히 `llm.py`의 캐시+재시도 계층과
   `review.py`의 관심사 학습 루프는 대체재가 없는 핵심 자산이다.
2. **식당 추천은 jaebin의 `restaurant.py`로 교체**하되, 출력 필드명을
   `FRONTEND.md` 계약(`name/category/price/near`)에 맞게 어댑터를 씌운다.
3. **서빙 방식(FastAPI vs 정적 JSON)은 코드 비교로 결정할 문제가 아니라
   프론트엔드 진행 상황과 발표 형식에 달린 제품 결정이다.** 라이브
   상호작용 데모가 필요하면 jaebin의 `server.py`를 얇은 어댑터로 남기고
   내부에서 김주환의 캐시된 `llm.call()`을 호출하도록 구성하는 것을
   권장한다(둘 다 살리는 방향). 정적 파일로도 충분하다면 김주환 방식을
   그대로 채택하고 jaebin의 `server.py`는 폐기하는 게 유지보수 부담이
   적다 — **이 결정은 두 작성자와 프론트엔드 담당자가 함께 논의해야
   한다.**
4. **스키마 통일이 병합 전 선결 과제다.** 성격 축 키 이름(영문 풀네임 vs
   단일문자), 관심사 표현(단일 필드 vs 이원화), 반복매칭 페널티 정책,
   환경변수 체계 네 가지는 코드를 합치기 전에 팀 회의로 하나씩 결론을
   내야 한다. 이 네 가지를 먼저 정하지 않고 병합을 시작하면 모든 모듈을
   두 번 고치게 된다.
5. **리스크가 가장 적은 순서**: (1) 환경변수 체계 통일 → (2) 성격축 키
   통일 → (3) 김주환의 `src/`를 새 패키지로 들여오고 jaebin의
   `restaurant.py`를 필드명만 조정해 이식 → (4) 서버 레이어를 얇은
   어댑터로 재작성(필요시) → (5) 관심사 이원화 스키마로 전체 전환.
   이 순서를 거꾸로(서버부터, 혹은 관심사 스키마부터) 하면 중간 상태에서
   두 스키마가 뒤섞여 디버깅이 어려워진다.

---

*이 문서는 두 브랜치의 코드를 읽고 비교 분석한 결과이며, 어떤 코드도
수정하지 않았습니다. worktree는 분석 완료 후 정리됩니다.*
